name: Triage Issues

on:
  issues:
    types: [opened, edited]
  issue_comment:
    types: [created]

concurrency:
  group: triage-issue-${{ github.event.issue.number }}
  cancel-in-progress: true

jobs:
  triage:
    if: github.event_name == 'issues' || (contains(github.event.issue.labels.*.name, 'needs-info') && github.event.comment.user.type == 'User')
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: read
    outputs:
      result: ${{ steps.agent.outputs.result }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: npm install --no-save --no-audit --no-fund software-factory-cli@__FACTORY_VERSION__
      - name: Load complete issue context
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          NUMBER: ${{ github.event.issue.number }}
          OUT: ${{ runner.temp }}/issue.json
        run: |
          gh issue view "$NUMBER" --repo "$REPO" --json number,title,body,labels,author,createdAt,url,comments > "$RUNNER_TEMP/raw-issue.json"
          node -e 'const fs=require("fs");const x=require(process.env.RUNNER_TEMP+"/raw-issue.json");x.labels=x.labels.map(v=>v.name);x.author=x.author.login;x.comments=x.comments.map(v=>({author:v.author.login,body:v.body,createdAt:v.createdAt}));fs.writeFileSync(process.env.OUT,JSON.stringify(x))'
      - name: Run real triage agent
        id: agent
        env:
          FACTORY_SYNC_LABELS: "0"
          FACTORY_STATE_DIR: ${{ runner.temp }}/factory-state
          FACTORY_GH_REPO: ${{ github.repository }}
          ANTHROPIC_AUTH_TOKEN: ${{ secrets.ANTHROPIC_AUTH_TOKEN }}
          ANTHROPIC_BASE_URL: ${{ secrets.ANTHROPIC_BASE_URL }}
          ANTHROPIC_MODEL: ${{ secrets.ANTHROPIC_MODEL }}
        run: |
          node node_modules/software-factory-cli/dist/factory/run-issue.js \
            --stage triage \
            --issue "$RUNNER_TEMP/issue.json" \
            --output-file "$RUNNER_TEMP/triage-result.json"
          echo "result=$(cat "$RUNNER_TEMP/triage-result.json")" >> "$GITHUB_OUTPUT"

  apply:
    needs: triage
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
      - name: Apply validated decision
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          NUMBER: ${{ github.event.issue.number }}
          RESULT: ${{ needs.triage.outputs.result }}
        run: |
          cp "$RUNNER_TEMP/triage-result.json" "$RUNNER_TEMP/result.json"
          jq -e '.triageResult.label and .triageResult.comment' "$RUNNER_TEMP/result.json" >/dev/null
          LABEL=$(jq -r '.triageResult.label' "$RUNNER_TEMP/result.json")
          COMMENT=$(jq -r '.triageResult.comment' "$RUNNER_TEMP/result.json")
          # Defence in depth: LLM-generated labels must match the canonical
          # factory label grammar, otherwise an upstream JSON-escape bug
          # could let a malicious label name reach `gh issue edit`.
          if ! printf '%s' "$LABEL" | grep -Eq '^[a-z0-9][a-z0-9 -]{0,49}$'; then
            echo "Refusing to apply unsafe label: $LABEL" >&2
            exit 1
          fi
          gh label create "$LABEL" --repo "$REPO" --color 5319E7 --force
          for OLD in ready-to-implement ready-to-spec spec-ready-for-review needs-info wait-to-implement review-needed ready-to-merge verified verify-failed changes-requested; do
            if [ "$OLD" != "$LABEL" ]; then gh issue edit "$NUMBER" --repo "$REPO" --remove-label "$OLD" 2>/dev/null || true; fi
          done
          gh issue edit "$NUMBER" --repo "$REPO" --add-label "$LABEL"
          gh issue comment "$NUMBER" --repo "$REPO" --body-file <(printf '%s' "$COMMENT")
